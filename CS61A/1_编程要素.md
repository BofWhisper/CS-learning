#关键知识点
1.函数的定义以及调用。
2.函数变量的作用域与函数的嵌套
3.纯功能与非纯函数print
eg1：
>>>print(print(1),print(2))
1
2
None None
eg2：
>>> two = print(2)
2
>>> print(two)
None
非纯函数：除了返回一个值外，应用非纯的函数可能产生副作用，从而对解释器或计算机的一个常见的副作用是产生额外的产出，超出返回值，使用打印函数。
print返回值是None，他的功能是打印。
