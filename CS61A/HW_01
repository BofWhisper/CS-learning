问题1：不用abs，求a＋b的绝对值
if  b < 0
	f = a - b
else 
	f = a+b
return f(a,b)
问题2：三个中最小两个数的平方和（一行代码）
return min(i*i+j*j,i*i+k*k,j*j+k*k)
问题3：写一个函数，取一个整数大于 1 且 返回最小且小于且整除的最大整数
def  MaxFactor(a):
	int i = 1
	while(i < a)
		if(a % i == 0)
			b = max(1,i)
		i = i +1
	return b
问题4：道格拉斯·霍夫施塔特的普利策奖得主著作《哥德尔、埃舍尔、巴赫》中，姿势 以下数学谜题。

选择一个正整数作为起始。n
如果是偶数，则除以2。n
如果是奇数，乘以3再加1。n
继续这个过程直到1。n
这个数字会上下波动，但最终会以1结束（至少对于 所有曾经尝试过的数字——从来没有人能证明 序列将终止）。类似地，冰雹在 大气层，最终降落地球。n

这串值通常被称为冰雹序列。写一个 函数取一个形式参数名为 的参数，输出 从Hilstone序列开始，返回在的步数def HilstoneStep(n):
    b = 1
    while n > 0 and n != 1:
        if n % 2 == 0:
            n = n / 2
        elif n % 2 == 1:
            n = n * 3 + 1
        b = b + 1
    return b  # 必须返回结果！
